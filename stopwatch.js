#!/usr/bin/env node
var timeFunctions = require('./timeFunctions');

//Create a stopwatch that counts to 10 using setTimeout.
//Create a stopwatch that counts to 10 using setReminder.
timeFunctions.countUsingTimeout();
